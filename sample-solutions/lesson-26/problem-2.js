// Write a program, using the variable declarations above, that displays the data type for each variable.

let number = 23;
let name = 'Mina';
let alive = true;

console.log(`Variable number is of data type ${typeof number}.`);
console.log(`Variable name is of data type ${typeof name}.`);
console.log(`Variable alive is of data type ${typeof alive}.`);